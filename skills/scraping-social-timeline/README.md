# scraping-social-timeline

This directory is the future publishable skill package for `skills.sh`.

Its job is to document workflow and runtime requirements, not to embed the
project's private implementation details.

The executable dependency is the `social-timeline` CLI provided by
`packages/social_timeline_runtime`.
